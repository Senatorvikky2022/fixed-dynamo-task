There is an access log at /app/data/access.log. Analyze the traffic and create a JSON report at /app/report.json that contains:

1. A "total_requests" field with the number of requests in the log
2. A "clients" array with objects containing "ip" and "request_count" for each client
3. A "popular_pages" array with objects containing "path" and "hit_count" for the top 10 most requested pages

The report.json must be valid JSON with the exact field names specified above.

You have 300 seconds to complete this task. Do not cheat by using online solutions or hints specific to this task.
